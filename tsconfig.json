{
  "compilerOptions": {
    "target": "es6",                          /* 컴파일 ECMAscript 대상 지정 */
    "module": "commonjs",                     /* 모듈 코드 생성 지정 */
    "outDir": "./build",                       /* 컴파일 경로 지정 */
    "rootDir": "./src",                       /* 컴파일할 대상의 root 디렉토리 지정(이에 맞춰서 컴파일 시 나오는 outDir구조가 똑같이 만들어짐) */
    "strict": true,                           /* 타입스크립트의 엄격한 유형 검사 활성화 여부 */
    "moduleResolution": "node",               /* node 또는 classic 모듈 분석 방식(?) */
    "esModuleInterop": true
  }
}
